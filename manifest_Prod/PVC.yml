apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs-prod-claim
  namespace: petclinic-prod
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: ebs-sc
